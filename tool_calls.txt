[
  {
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Add full course names mapping and display",
      "EndLine": 250,
      "Instruction": "Add the courseFullNames dictionary, and update the section header (the <h3> that displays {course}) to also display the full formal name if it exists in the dictionary.",
      "ReplacementContent": "const courseFullNames: Record<string, string> = {\n  'ANTHR': 'Anthropology',\n  'ARCHI': 'Architecture',\n  'BIOSC': 'Biological Science',\n  'BUS': 'Business',\n  'BUSAC': 'Business Accounting',\n  'CHEM': 'Chemistry',\n  'COMM': 'Communication Studies',\n  'COMSC': 'Computer Science',\n  'ECON': 'Economics',\n  'ENGL': 'English',\n  'HIST': 'History',\n  'MATH': 'Mathematics',\n  'PHYS': 'Physics',\n  'PSYCH': 'Psychology',\n  'SOCIO': 'Sociology',\n  'POLSC': 'Political Science',\n  'MUSIC': 'Music',\n  'ART': 'Art',\n  'JAPN': 'Japanese',\n  'KNACT': 'Kinesiology Activity',\n  'ENACT': 'Kinesiology Activity',\n  'ESL': 'English as a Second Language',\n  'ETHNIC': 'Ethnic Studies',\n  'FTVE': 'Film, Television, and Electronic Media',\n  'GEOG': 'Geography',\n  'INTD': 'Interior Design',\n  'MARKETING': 'Marketing',\n  'COUNSELING': 'Counseling',\n  'DANCE': 'Dance'\n};\n\nexport default function Home() {\n  const [activeTab, setActiveTab] = useState<'course' | 'prof'>('course');\n  const [searchCourse, setSearchCourse] = useState(\"\");\n  const [searchProf, setSearchProf] = useState(\"\");\n  const [sortBy, setSortBy] = useState<SortOption>('reviews');\n  const [geFilter, setGeFilter] = useState<string[]>([]); // New state for GE Area filtering\n\n  // Modals / Overlays\n  const [selectedProfDetails, setSelectedProfDetails] = useState<{profName: string, reviews: Review[]} | null>(null);\n  const [compareList, setCompareList] = useState<{profName: string, reviews: Review[]}[]>([]);\n  const [showCompareModal, setShowCompareModal] = useState(false);\n\n  const uniqueCourses = useMemo(() => Array.from(new Set(mockData.map(r => r.course).filter(Boolean))).sort(), []);\n  const uniqueProfs = useMemo(() => Array.from(new Set(mockData.map(r => r.profName).filter(Boolean))).sort(), []);\n\n  const filteredData = useMemo(() => {\n    if (activeTab === 'course') {\n      let data = mockData;\n      // Filter by GE Area if active\n      if (geFilter.length > 0) {\n        data = data.filter(r => geFilter.includes(r.course.toUpperCase()));\n      }\n      // Filter by text search if present\n      if (searchCourse) {\n        const exactPrefixMatch = uniqueCourses.find(uc => uc.toLowerCase() === searchCourse.toLowerCase());\n        if (exactPrefixMatch) {\n          data = data.filter(r => r.course === exactPrefixMatch);\n        } else {\n          data = data.filter(r => r.courseFull.toLowerCase().includes(searchCourse.toLowerCase()) || r.course.toLowerCase().includes(searchCourse.toLowerCase()));\n        }\n      }\n      return data;\n    } else {\n      if (!searchProf) return [];\n      return mockData.filter(r => r.profName.toLowerCase().includes(searchProf.toLowerCase()));\n    }\n  }, [activeTab, searchCourse, searchProf, geFilter, uniqueCourses]);\n\n  const courseGrouped = useMemo(() => {\n    const groups: Record<string, Record<string, Review[]>> = {};\n    filteredData.forEach(r => {\n      if (!r.course) return;\n      if (!groups[r.course]) groups[r.course] = {};\n      if (!groups[r.course][r.profName]) groups[r.course][r.profName] = [];\n      groups[r.course][r.profName].push(r);\n    });\n    return groups;\n  }, [filteredData]);\n\n  const profGrouped = useMemo(() => {\n    const groups: Record<string, Record<string, Review[]>> = {};\n    filteredData.forEach(r => {\n      if (!r.profName) return;\n      if (!groups[r.profName]) groups[r.profName] = {};\n      if (!groups[r.profName][r.course]) groups[r.profName][r.course] = [];\n      groups[r.profName][r.course].push(r);\n    });\n    return groups;\n  }, [filteredData]);\n\n  const calculateAverages = (reviews: Review[]) => {\n    const sum = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => reviews.reduce((a, b) => a + (b[key] || 0), 0);\n    const count = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => reviews.filter(r => r[key] > 0).length;\n    const avg = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => {\n      const c = count(key);\n      return c > 0 ? (sum(key) / c).toFixed(1) : \"0.0\";\n    };\n    return { prof: avg('ratingProf'), cls: avg('ratingClass'), easy: avg('ratingEasy') };\n  };\n\n  const toggleCompare = (profName: string, reviews: Review[]) => {\n    setCompareList(prev => {\n      if (prev.find(p => p.profName === profName)) {\n        return prev.filter(p => p.profName !== profName);\n      } else {\n        if (prev.length >= 4) {\n          alert(\"豈碑ｼ・〒縺阪ｋ縺ｮ縺ｯ譛螟ｧ4蜷阪∪縺ｧ縺ｧ縺吶・");\n          return prev;\n        }\n        return [...prev, { profName, reviews }];\n      }\n    });\n  };\n\n  // Prevent background scrolling when modal is open\n  useEffect(() => {\n    if (selectedProfDetails || showCompareModal) {\n      document.body.style.overflow = 'hidden';\n    } else {\n      document.body.style.overflow = 'unset';\n    }\n  }, [selectedProfDetails, showCompareModal]);\n\n  return (\n    <div className=\"min-h-screen bg-[#ecebe8] text-[#1a162d] font-sans pb-32 relative\">\n      <header className=\"pt-12 pb-16 px-8\">\n        <div className=\"max-w-6xl mx-auto flex justify-center items-center\">\n          <div className=\"font-serif text-2xl tracking-[0.2em] italic font-light cursor-pointer\" onClick={() => { setSearchCourse(\"\"); setSearchProf(\"\"); }}>ProRater.</div>\n        </div>\n      </header>\n\n      <main className=\"max-w-5xl mx-auto px-6\">\n        \n        {/* Intent Tabs */}\n        <div className=\"flex justify-center gap-12 mb-16\">\n          <button \n            onClick={() => setActiveTab('course')}\n            className={`pb-2 text-sm tracking-[0.1em] transition-all duration-500 relative ${activeTab === 'course' ? 'text-[#1a162d]' : 'text-[#8c8a99] hover:text-[#1a162d]'}`}\n          >\n            蛻・㍽繝ｻ遘醍岼縺九ｉ謗｢縺兔n            {activeTab === 'course' && <span className=\"absolute left-0 bottom-0 w-full h-[1px] bg-[#1a162d]\"></span>}\n          </button>\n          <button \n            onClick={() => setActiveTab('prof')}\n            className={`pb-2 text-sm tracking-[0.1em] transition-all duration-500 relative ${activeTab === 'prof' ? 'text-[#1a162d]' : 'text-[#8c8a99] hover:text-[#1a162d]'}`}\n          >\n            謨呎肢蜷阪°繧画爾縺兔n            {activeTab === 'prof' && <span className=\"absolute left-0 bottom-0 w-full h-[1px] bg-[#1a162d]\"></span>}\n          </button>\n        </div>\n\n        {/* Search Area */}\n        <div className=\"mb-24 max-w-4xl mx-auto relative group\">\n          {activeTab === 'course' ? (\n            <>\n              <input \n                type=\"text\" \n                list=\"courses\"\n                value={searchCourse}\n                onChange={(e) => setSearchCourse(e.target.value)}\n                placeholder=\"遘醍岼蜷阪・蛻・㍽繧貞・蜉媾"\n                className=\"w-full max-w-3xl mx-auto block bg-transparent border-b border-[#1a162d]/20 py-4 text-3xl md:text-5xl font-serif font-light focus:outline-none focus:border-[#1a162d] transition-colors text-center text-[#1a162d] placeholder:text-[#1a162d]/20\"\n              />\n              <datalist id=\"courses\">\n                {uniqueCourses.map(c => <option key={c} value={c} />)}\n              </datalist>\n              \n              {/* Quick Tags Section */}\n              <div className=\"mt-12 animate-fade-in-up\">\n                <div className=\"text-center mb-6 text-xs tracking-[0.2em] text-[#5a5866] font-medium uppercase\">荳ｻ隕√↑蛻・㍽縺九ｉ謗｢縺・/div>\n                <div className=\"flex flex-wrap justify-center gap-3 mb-10\">\n                  {['MATH', 'ENGL', 'PHYS', 'CHEM', 'BIOSC', 'BUS', 'ECON', 'HIST', 'COMSC', 'ANTHR', 'PSYCH', 'SOCIO', 'ART'].filter(s => uniqueCourses.includes(s)).map(subj => (\n                    <button \n                      key={subj}\n                      onClick={() => { setSearchCourse(subj); setGeFilter([]); }} \n                      className={`px-5 py-2 rounded-full border text-xs font-medium tracking-widest transition-colors shadow-sm ${searchCourse === subj && geFilter.length === 0 ? 'bg-[#1a162d] border-[#1a162d] text-white' : 'border-[#1a162d]/15 text-[#3a3845] hover:border-[#1a162d] hover:text-[#1a162d] bg-white/50'}`}\n                    >\n                      {subj}\n                    </button>\n                  ))}\n                  <button \n                    onClick={() => { setSearchCourse(\"\"); setGeFilter([]); }}\n                    className={`px-5 py-2 rounded-full border text-xs font-medium tracking-widest transition-colors ${!searchCourse && geFilter.length === 0 ? 'bg-[#e2e1dd] border-transparent text-[#1a162d]' : 'border-transparent text-[#5a5866] hover:text-[#1a162d]'}`}\n                  >\n                    縺吶∋縺ｦ陦ｨ遉ｺ\n                  </button>\n                </div>\n\n                <div className=\"text-center mb-6 text-xs tracking-[0.2em] text-[#5a5866] font-medium uppercase\">Cal-GETC / IGETC 繧ｨ繝ｪ繧｢縺九ｉ謗｢縺・/div>\n                <div className=\"flex flex-wrap justify-center gap-3\">\n                  {[\n                    { label: 'Area 1 (English)', keywords: ['ENGL', 'COMM'] },\n                    { label: 'Area 2 (Math)', keywords: ['MATH', 'BUS', 'STAT', 'BUSAC'] },\n                    { label: 'Area 3 (Arts & Humanities)', keywords: ['ART', 'ARTHS', 'MUSIC', 'DRAMA', 'HIST', 'HUMAN', 'PHILO', 'ENGL', 'SPAN', 'JAPAN'] },\n                    { label: 'Area 4 (Social Sciences)', keywords: ['ANTHR', 'ECON', 'SOCIO', 'POLS', 'PSYCH', 'HIST', 'GEOG', 'ETHNIC'] },\n                    { label: 'Area 5 (Sciences)', keywords: ['PHYS', 'CHEM', 'BIOSC', 'ASTRO', 'GEOL', 'ANTHR'] },\n                  ].map(area => (\n                    <button \n                      key={area.label}\n                      onClick={() => { setSearchCourse(\"\"); setGeFilter(area.keywords); }} \n                      className={`px-5 py-2 rounded-full border text-xs font-medium tracking-wide transition-colors shadow-sm ${geFilter === area.keywords ? 'bg-[#1a162d] border-[#1a162d] text-white' : 'border-[#1a162d]/15 text-[#3a3845] hover:bg-[#1a162d] hover:border-[#1a162d] hover:text-white bg-white/50'}`}\n                    >\n                      {area.label}\n                    </button>\n                  ))}\n                </div>\n              </div>\n            </>\n          ) : (\n            <>\n              <input \n                type=\"text\" \n                list=\"profs\"\n                value={searchProf}\n                onChange={(e) => setSearchProf(e.target.value)}\n                placeholder=\"謨呎肢縺ｮ蜷榊燕繧貞・蜉媾"\n                className=\"w-full bg-transparent border-b border-[#1a162d]/20 py-4 text-3xl md:text-5xl font-serif font-light focus:outline-none focus:border-[#1a162d] transition-colors text-center text-[#1a162d] placeholder:text-[#1a162d]/20\"\n              />\n              <datalist id=\"profs\">\n                {uniqueProfs.map(p => <option key={p} value={p} />)}\n              </datalist>\n            </>\n          )}\n        </div>\n\n        {/* Results Area */}\n        <div>\n          {/* COURSE RESULTS */}\n          {activeTab === 'course' && searchCourse && (\n            Object.keys(courseGrouped).length === 0 ? (\n              <div className=\"text-center py-12 text-[#8c8a99] tracking-widest text-sm font-light\">邨先棡縺瑚ｦ九▽縺九ｊ縺ｾ縺帙ｓ縲・/div>\n            ) : (\n              <div className=\"space-y-32\">\n                {Object.entries(courseGrouped).map(([course, profGroups]) => {\n                  \n                  // Sorting Logic\n                  const sortedProfs = Object.entries(profGroups).map(([profName, reviews]) => {\n                    const avgs = calculateAverages(reviews);\n                    return { profName, reviews, avgs };\n                  }).sort((a, b) => {\n                    if (sortBy === 'reviews') return b.reviews.length - a.reviews.length;\n                    if (sortBy === 'profRating') return parseFloat(b.avgs.prof) - parseFloat(a.avgs.prof);\n                    if (sortBy === 'easyRating') return parseFloat(b.avgs.easy) - parseFloat(a.avgs.easy);\n                    return 0;\n                  });\n\n                  return (\n                    <div key={course}>\n                      {/* Section Header with Inline Sort */}\n                      <div className=\"flex flex-col md:flex-row justify-between items-end mb-12 border-b border-[#1a162d]/10 pb-6\">\n                        <div>\n                          <h3 className=\"text-4xl md:text-5xl font-serif text-[#1a162d] mb-2 flex items-baseline\">\n                            {course} \n                            {courseFullNames[course] && (\n                              <span className=\"text-lg md:text-2xl text-[#8c8a99] font-sans font-light ml-4 tracking-wide\">\n                                ({courseFullNames[course]})\n                              </span>\n                            )}\n                          </h3>\n                          <span className=\"text-xs text-[#8c8a99] tracking-[0.2em] uppercase\">\n                            {sortedProfs.length}蜷阪・謨呎肢\n                          </span>\n                        </div>",
      "StartLine": 11,
      "TargetContent": "export default function Home() {\n  const [activeTab, setActiveTab] = useState<'course' | 'prof'>('course');\n  const [searchCourse, setSearchCourse] = useState(\"\");\n  const [searchProf, setSearchProf] = useState(\"\");\n  const [sortBy, setSortBy] = useState<SortOption>('reviews');\n  const [geFilter, setGeFilter] = useState<string[]>([]); // New state for GE Area filtering\n\n  // Modals / Overlays\n  const [selectedProfDetails, setSelectedProfDetails] = useState<{profName: string, reviews: Review[]} | null>(null);\n  const [compareList, setCompareList] = useState<{profName: string, reviews: Review[]}[]>([]);\n  const [showCompareModal, setShowCompareModal] = useState(false);\n\n  const uniqueCourses = useMemo(() => Array.from(new Set(mockData.map(r => r.course).filter(Boolean))).sort(), []);\n  const uniqueProfs = useMemo(() => Array.from(new Set(mockData.map(r => r.profName).filter(Boolean))).sort(), []);\n\n  const filteredData = useMemo(() => {\n    if (activeTab === 'course') {\n      let data = mockData;\n      // Filter by GE Area if active\n      if (geFilter.length > 0) {\n        data = data.filter(r => geFilter.includes(r.course.toUpperCase()));\n      }\n      // Filter by text search if present\n      if (searchCourse) {\n        const exactPrefixMatch = uniqueCourses.find(uc => uc.toLowerCase() === searchCourse.toLowerCase());\n        if (exactPrefixMatch) {\n          data = data.filter(r => r.course === exactPrefixMatch);\n        } else {\n          data = data.filter(r => r.courseFull.toLowerCase().includes(searchCourse.toLowerCase()) || r.course.toLowerCase().includes(searchCourse.toLowerCase()));\n        }\n      }\n      return data;\n    } else {\n      if (!searchProf) return [];\n      return mockData.filter(r => r.profName.toLowerCase().includes(searchProf.toLowerCase()));\n    }\n  }, [activeTab, searchCourse, searchProf, geFilter, uniqueCourses]);\n\n  const courseGrouped = useMemo(() => {\n    const groups: Record<string, Record<string, Review[]>> = {};\n    filteredData.forEach(r => {\n      if (!r.course) return;\n      if (!groups[r.course]) groups[r.course] = {};\n      if (!groups[r.course][r.profName]) groups[r.course][r.profName] = [];\n      groups[r.course][r.profName].push(r);\n    });\n    return groups;\n  }, [filteredData]);\n\n  const profGrouped = useMemo(() => {\n    const groups: Record<string, Record<string, Review[]>> = {};\n    filteredData.forEach(r => {\n      if (!r.profName) return;\n      if (!groups[r.profName]) groups[r.profName] = {};\n      if (!groups[r.profName][r.course]) groups[r.profName][r.course] = [];\n      groups[r.profName][r.course].push(r);\n    });\n    return groups;\n  }, [filteredData]);\n\n  const calculateAverages = (reviews: Review[]) => {\n    const sum = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => reviews.reduce((a, b) => a + (b[key] || 0), 0);\n    const count = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => reviews.filter(r => r[key] > 0).length;\n    const avg = (key: 'ratingProf' | 'ratingClass' | 'ratingEasy') => {\n      const c = count(key);\n      return c > 0 ? (sum(key) / c).toFixed(1) : \"0.0\";\n    };\n    return { prof: avg('ratingProf'), cls: avg('ratingClass'), easy: avg('ratingEasy') };\n  };\n\n  const toggleCompare = (profName: string, reviews: Review[]) => {\n    setCompareList(prev => {\n      if (prev.find(p => p.profName === profName)) {\n        return prev.filter(p => p.profName !== profName);\n      } else {\n        if (prev.length >= 4) {\n          alert(\"豈碑ｼ・〒縺阪ｋ縺ｮ縺ｯ譛螟ｧ4蜷阪∪縺ｧ縺ｧ縺吶・");\n          return prev;\n        }\n        return [...prev, { profName, reviews }];\n      }\n    });\n  };\n\n  // Prevent background scrolling when modal is open\n  useEffect(() => {\n    if (selectedProfDetails || showCompareModal) {\n      document.body.style.overflow = 'hidden';\n    } else {\n      document.body.style.overflow = 'unset';\n    }\n  }, [selectedProfDetails, showCompareModal]);\n\n  return (\n    <div className=\"min-h-screen bg-[#ecebe8] text-[#1a162d] font-sans pb-32 relative\">\n      <header className=\"pt-12 pb-16 px-8\">\n        <div className=\"max-w-6xl mx-auto flex justify-center items-center\">\n          <div className=\"font-serif text-2xl tracking-[0.2em] italic font-light cursor-pointer\" onClick={() => { setSearchCourse(\"\"); setSearchProf(\"\"); }}>ProRater.</div>\n        </div>\n      </header>\n\n      <main className=\"max-w-5xl mx-auto px-6\">\n        \n        {/* Intent Tabs */}\n        <div className=\"flex justify-center gap-12 mb-16\">\n          <button \n            onClick={() => setActiveTab('course')}\n            className={`pb-2 text-sm tracking-[0.1em] transition-all duration-500 relative ${activeTab === 'course' ? 'text-[#1a162d]' : 'text-[#8c8a99] hover:text-[#1a162d]'}`}\n          >\n            蛻・㍽繝ｻ遘醍岼縺九ｉ謗｢縺兔n            {activeTab === 'course' && <span className=\"absolute left-0 bottom-0 w-full h-[1px] bg-[#1a162d]\"></span>}\n          </button>\n          <button \n            onClick={() => setActiveTab('prof')}\n            className={`pb-2 text-sm tracking-[0.1em] transition-all duration-500 relative ${activeTab === 'prof' ? 'text-[#1a162d]' : 'text-[#8c8a99] hover:text-[#1a162d]'}`}\n          >\n            謨呎肢蜷阪°繧画爾縺兔n            {activeTab === 'prof' && <span className=\"absolute left-0 bottom-0 w-full h-[1px] bg-[#1a162d]\"></span>}\n          </button>\n        </div>\n\n        {/* Search Area */}\n        <div className=\"mb-24 max-w-4xl mx-auto relative group\">\n          {activeTab === 'course' ? (\n            <>\n              <input \n                type=\"text\" \n                list=\"courses\"\n                value={searchCourse}\n                onChange={(e) => setSearchCourse(e.target.value)}\n                placeholder=\"遘醍岼蜷阪・蛻・㍽繧貞・蜉媾"\n                className=\"w-full max-w-3xl mx-auto block bg-transparent border-b border-[#1a162d]/20 py-4 text-3xl md:text-5xl font-serif font-light focus:outline-none focus:border-[#1a162d] transition-colors text-center text-[#1a162d] placeholder:text-[#1a162d]/20\"\n              />\n              <datalist id=\"courses\">\n                {uniqueCourses.map(c => <option key={c} value={c} />)}\n              </datalist>\n              \n              {/* Quick Tags Section */}\n              <div className=\"mt-12 animate-fade-in-up\">\n                <div className=\"text-center mb-6 text-xs tracking-[0.2em] text-[#5a5866] font-medium uppercase\">荳ｻ隕√↑蛻・㍽縺九ｉ謗｢縺・/div>\n                <div className=\"flex flex-wrap justify-center gap-3 mb-10\">\n                  {['MATH', 'ENGL', 'PHYS', 'CHEM', 'BIOSC', 'BUS', 'ECON', 'HIST', 'COMSC', 'ANTHR', 'PSYC', 'SOCIO', 'ART'].filter(s => uniqueCourses.includes(s)).map(subj => (\n                    <button \n                      key={subj}\n                      onClick={() => { setSearchCourse(subj); setGeFilter([]); }} \n                      className={`px-5 py-2 rounded-full border text-xs font-medium tracking-widest transition-colors shadow-sm ${searchCourse === subj && geFilter.length === 0 ? 'bg-[#1a162d] border-[#1a162d] text-white' : 'border-[#1a162d]/15 text-[#3a3845] hover:border-[#1a162d] hover:text-[#1a162d] bg-white/50'}`}\n                    >\n                      {subj}\n                    </button>\n                  ))}\n                  <button \n                    onClick={() => { setSearchCourse(\"\"); setGeFilter([]); }}\n                    className={`px-5 py-2 rounded-full border text-xs font-medium tracking-widest transition-colors ${!searchCourse && geFilter.length === 0 ? 'bg-[#e2e1dd] border-transparent text-[#1a162d]' : 'border-transparent text-[#5a5866] hover:text-[#1a162d]'}`}\n                  >\n                    縺吶∋縺ｦ陦ｨ遉ｺ\n                  </button>\n                </div>\n\n                <div className=\"text-center mb-6 text-xs tracking-[0.2em] text-[#5a5866] font-medium uppercase\">Cal-GETC / IGETC 繧ｨ繝ｪ繧｢縺九ｉ謗｢縺・/div>\n                <div className=\"flex flex-wrap justify-center gap-3\">\n                  {[\n                    { label: 'Area 1 (English)', keywords: ['ENGL', 'COMM'] },\n                    { label: 'Area 2 (Math)', keywords: ['MATH', 'BUS', 'STAT', 'BUSAC'] },\n                    { label: 'Area 3 (Arts & Humanities)', keywords: ['ART', 'ARTHS', 'MUSIC', 'DRAMA', 'HIST', 'HUMAN', 'PHILO', 'ENGL', 'SPAN', 'JAPAN'] },\n                    { label: 'Area 4 (Social Sciences)', keywords: ['ANTHR', 'ECON', 'SOCIO', 'POLS', 'PSYC', 'HIST', 'GEOG', 'ETHNIC'] },\n                    { label: 'Area 5 (Sciences)', keywords: ['PHYS', 'CHEM', 'BIOSC', 'ASTRO', 'GEOL', 'ANTHR'] },\n                  ].map(area => (\n                    <button \n                      key={area.label}\n                      onClick={() => { setSearchCourse(\"\"); setGeFilter(area.keywords); }} \n                      className={`px-5 py-2 rounded-full border text-xs font-medium tracking-wide transition-colors shadow-sm ${geFilter === area.keywords ? 'bg-[#1a162d] border-[#1a162d] text-white' : 'border-[#1a162d]/15 text-[#3a3845] hover:bg-[#1a162d] hover:border-[#1a162d] hover:text-white bg-white/50'}`}\n                    >\n                      {area.label}\n                    </button>\n                  ))}\n                </div>\n              </div>\n            </>\n          ) : (\n            <>\n              <input \n                type=\"text\" \n                list=\"profs\"\n                value={searchProf}\n                onChange={(e) => setSearchProf(e.target.value)}\n                placeholder=\"謨呎肢縺ｮ蜷榊燕繧貞・蜉媾"\n                className=\"w-full bg-transparent border-b border-[#1a162d]/20 py-4 text-3xl md:text-5xl font-serif font-light focus:outline-none focus:border-[#1a162d] transition-colors text-center text-[#1a162d] placeholder:text-[#1a162d]/20\"\n              />\n              <datalist id=\"profs\">\n                {uniqueProfs.map(p => <option key={p} value={p} />)}\n              </datalist>\n            </>\n          )}\n        </div>\n\n        {/* Results Area */}\n        <div>\n          {/* COURSE RESULTS */}\n          {activeTab === 'course' && searchCourse && (\n            Object.keys(courseGrouped).length === 0 ? (\n              <div className=\"text-center py-12 text-[#8c8a99] tracking-widest text-sm font-light\">邨先棡縺瑚ｦ九▽縺九ｊ縺ｾ縺帙ｓ縲・/div>\n            ) : (\n              <div className=\"space-y-32\">\n                {Object.entries(courseGrouped).map(([course, profGroups]) => {\n                  \n                  // Sorting Logic\n                  const sortedProfs = Object.entries(profGroups).map(([profName, reviews]) => {\n                    const avgs = calculateAverages(reviews);\n                    return { profName, reviews, avgs };\n                  }).sort((a, b) => {\n                    if (sortBy === 'reviews') return b.reviews.length - a.reviews.length;\n                    if (sortBy === 'profRating') return parseFloat(b.avgs.prof) - parseFloat(a.avgs.prof);\n                    if (sortBy === 'easyRating') return parseFloat(b.avgs.easy) - parseFloat(a.avgs.easy);\n                    return 0;\n                  });\n\n                  return (\n                    <div key={course}>\n                      {/* Section Header with Inline Sort */}\n                      <div className=\"flex flex-col md:flex-row justify-between items-end mb-12 border-b border-[#1a162d]/10 pb-6\">\n                        <div>\n                          <h3 className=\"text-4xl md:text-5xl font-serif text-[#1a162d] mb-2\">{course}</h3>\n                          <span className=\"text-xs text-[#8c8a99] tracking-[0.2em] uppercase\">\n                            {sortedProfs.length}蜷阪・謨呎肢\n                          </span>\n                        </div>",
      "TargetFile": "c:\\Users\\keitaro\\Desktop\\DVC_JA_Rating_Project\\prorater\\src\\app\\page.tsx",
      "TargetLintErrorIds": [],
      "toolAction": "Add full names mapping",
      "toolSummary": "Update page.tsx"
    }
  }
]
