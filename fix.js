const fs = require('fs');
fs.writeFileSync('src/app/page.tsx', 'import { supabase } from \'../lib/supabase\';\nimport ClientPage from \'./ClientPage\';\n\nexport const revalidate = 3600;\n\nexport default async function Page() {\n  const { data, error } = await supabase.from(\'reviews\').select(\'*\');\n  if (error) {\n    console.error(\'Error fetching:\', error);\n    return <div>Error loading data.</div>;\n  }\n  return <ClientPage initialData={data || []} />;\n}\n');
fs.writeFileSync('src/lib/supabase.ts', 'import { createClient } from \'@supabase/supabase-js\';\nconst supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || \'https://cuqzrphvqiaaprqvfzdz.supabase.co\';\nconst supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || \'sb_publishable_SqGX4F7Zv9lrkRHiesX5AQ_sBT2q1JC\';\nexport const supabase = createClient(supabaseUrl, supabaseKey);\n');
